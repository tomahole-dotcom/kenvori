import test from "node:test";import assert from "node:assert/strict";import {factoryRunSummary} from "../src/production-runner.js";
test("production runner stays blocked without real catalog economics",()=>{const r=factoryRunSummary();assert.equal(r.productionPlans,0);assert.equal(r.next,"NEEDS_LIVE_CATALOG_ECONOMICS_OR_ARTWORK_LOCK");assert.equal(r.safeMode,true)});
test("research seed plus approved live poster reaches locked artwork chain",()=>{const r=factoryRunSummary({catalogEconomics:[{productType:"poster",blueprintId:1,providerId:2,variantId:3,priceCents:2999,productionCostCents:531,shippingCostCents:599,economics:{approved:true,marginPct:50.32}}]});assert.ok(r.productionPlans>0);assert.equal(r.next,"GENERATE_LOCKED_ARTWORK");const p=r.plans[0];assert.equal(p.status,"ARTWORK_CHAIN_LOCKED");assert.equal(p.artworkChain.approved,true);assert.equal(p.artworkValidation.ok,true);assert.ok(p.artworkChain.locked.researchRef);assert.ok(p.artworkChain.locked.opportunityTheme);assert.ok(p.artworkChain.locked.creativeConcept);assert.equal(p.product.productType,"poster");assert.equal(p.safeMode,true);assert.equal(p.publishAuthorization,false)});