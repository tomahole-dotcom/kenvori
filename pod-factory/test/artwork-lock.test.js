import test from "node:test";import assert from "node:assert/strict";import {lockArtworkChain,validateArtworkRequest} from "../src/artwork-lock.js";
const base={researchRef:"pinterest_predicts_2026:circus interior",opportunityTheme:"FunHaus elevated circus",creativeConcept:"FunHaus elevated circus — surreal graphic poster with refined theatrical geometry, striped architectural forms and playful abstract circus energy; no branded characters",visualMotif:"refined theatrical geometry with striped architectural forms, abstract rings and balanced playful circus energy; no animals, text or scenery",productType:"poster",blueprintId:97,providerId:99,variantId:33742,pricing:{priceCents:2999,productionCostCents:531,shippingCostCents:599},forbiddenThemes:["Norway","fjord","travel"]};
test("locks complete research-led chain",()=>{const x=lockArtworkChain(base);assert.equal(x.approved,true);assert.equal(x.status,"ARTWORK_CHAIN_LOCKED");assert.match(x.artworkPrompt,/FunHaus elevated circus/);assert.equal(validateArtworkRequest(x,x.artworkPrompt).ok,true)});
test("blocks theme drift",()=>{const x=lockArtworkChain(base);const v=validateArtworkRequest(x,x.artworkPrompt+" Norway fjord travel");assert.equal(v.ok,false);assert.equal(v.status,"ARTWORK_BLOCKED")});
test("blocks incomplete or mismatched creative chain but does not use economics as a gate",()=>{assert.equal(lockArtworkChain({...base,pricing:null}).approved,true);assert.equal(lockArtworkChain({...base,creativeConcept:"generic mountain landscape"}).approved,false);assert.equal(lockArtworkChain({...base,visualMotif:""}).approved,false)});
