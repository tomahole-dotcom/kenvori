import test from "node:test";import assert from "node:assert/strict";import {firstCandidateQueue} from "../src/first-live-candidates.js";test("first real candidate batch is ranked and creative-preflighted",()=>{const q=firstCandidateQueue();assert.ok(q.length>=4);assert.ok(q[0].opportunity.score>=q.at(-1).opportunity.score);assert.ok(q.every(x=>x.preflight));assert.ok(q.some(x=>x.queueStatus==="READY_FOR_PRODUCT_MATCH"))});