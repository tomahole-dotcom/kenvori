import test from "node:test";import assert from "node:assert/strict";import {mergeSignals,deriveOpportunity} from "../src/discovery-signals.js";import {fromGoogleTrends,fromEtsyMarket} from "../src/discovery-adapters.js";
test("merges independent market evidence into one opportunity",()=>{const a=fromGoogleTrends([{query:"nurse shift humor",productType:"mug",audience:"nurses",theme:"shift humor",interest:80,growthPct:20}]);const b=fromEtsyMarket([{query:"nurse shift humor",productType:"mug",audience:"nurses",theme:"shift humor",competitionScore:40,medianPrice:25,reviewVelocity:70}]);const g=mergeSignals([...a,...b])[0],o=deriveOpportunity(g);assert.equal(g.sourceCount,2);assert.ok(o.demand>50);assert.equal(o.competitionGap,60)});
test("unknown metrics stay neutral instead of fabricated",()=>{const o=deriveOpportunity(mergeSignals([{source:"x",query:"q",productType:"poster"}])[0]);assert.equal(o.competitionGap,50);assert.equal(o.marginPotential,50)});