import test from "node:test";import assert from "node:assert/strict";import {candidate0002,candidate0002Gate} from "../src/candidate-0002.js";
test("candidate 0002 stays automated and safe",()=>{assert.equal(candidate0002.automation.manualDesignApprovalRequired,false);assert.equal(candidate0002.automation.safeMode,true);assert.equal(candidate0002.automation.publishAuthorization,false);assert.equal(candidate0002.creativeSpec.chooseBestAutomatically,true);assert.equal(candidate0002.creativeSpec.allowMultipleWinningConcepts,true);assert.equal(candidate0002.creativeSpec.productExpansion.allowMultipleProducts,true);assert.equal(candidate0002.decision.productFamily,"research_selected")});
test("candidate 0002 cannot reach publish gate with missing proof",()=>{const g=candidate0002Gate({catalogMatched:true});assert.equal(g.readyForPublishGate,false);assert.ok(g.missing.includes("actualCostKnown"));});
