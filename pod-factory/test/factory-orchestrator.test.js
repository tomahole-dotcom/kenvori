import test from "node:test";import assert from "node:assert/strict";import {runFactoryDiscovery} from "../src/factory-orchestrator.js";
test("research can produce multiple creative winners and products",()=>{const r=runFactoryDiscovery({signals:[{source:"x",query:"q",audience:"a",theme:"t",searchInterest:90,growth:40,competition:20,priceMedian:30,reviewVelocity:80}],productEvidence:[{name:"poster",theme:"t",researchEvidence:80,designFit:85,marginPotential:70},{name:"tote",theme:"t",researchEvidence:75,designFit:75,marginPotential:65}]});assert.equal(r.length,1);assert.ok(r[0].winners.length>1);assert.ok(r[0].winners[0].products.length>1);assert.equal(r[0].status,"READY_FOR_ECONOMICS")});
