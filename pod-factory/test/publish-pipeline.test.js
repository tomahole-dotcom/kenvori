import test from "node:test";import assert from "node:assert/strict";import {publishManifest} from "../src/publish-pipeline.js";
test("generic publisher fails closed before publish when authorization absent",async()=>{const r=await publishManifest({candidateKey:"x",printifyProductId:"x",expectedTitle:"x",expectedTags:[],expectedPriceCents:1,expectedTaxonomyId:1,qaPass:true,ipGreen:true,economicsApproved:true,publishAuthorization:false,printifyShopId:28992579});assert.equal(r.ok,false);assert.equal(r.publishCalled,false);assert.equal(r.stage,"GATE")});
test("generic publisher blocks wrong Printify shop",async()=>{const r=await publishManifest({candidateKey:"x",printifyProductId:"x",expectedTitle:"x",expectedTags:[],expectedPriceCents:1,expectedTaxonomyId:1,qaPass:true,ipGreen:true,economicsApproved:true,publishAuthorization:true,printifyShopId:4788136});assert.equal(r.publishCalled,false);assert.equal(r.reason,"WRONG_PRINTIFY_SHOP")});