import test from "node:test";import assert from "node:assert/strict";import {liveResearchCreativeBatch} from "../src/live-factory-run.js";
test("live batch fails closed or returns distinctive creative candidates",()=>{const r=liveResearchCreativeBatch();assert.ok(r.length>0);assert.ok(r.every(x=>x.opportunity.decision!=="REJECT"));for(const x of r){assert.ok(["READY_FOR_PRODUCT_RESEARCH","CREATIVE_HOLD"].includes(x.status));for(const c of x.concepts){assert.ok(c.creativeScore.approved);assert.ok(c.creativeScore.distinctiveness>=70);assert.ok(c.creativeScore.hookStrength>=70);}}});
